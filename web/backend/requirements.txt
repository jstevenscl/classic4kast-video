fastapi==0.115.5
uvicorn[standard]==0.32.1
httpx==0.27.2
pydantic==2.9.2
python-multipart==0.0.17
cryptography==43.0.3
# Already present transitively via uvicorn[standard] (backs FastAPI's own
# @app.websocket() support) -- pinned explicitly now that backend code
# also imports it directly as a CLIENT (web_channel_routes.py's
# login-session bridge connects outbound to webchannel-renderer).
websockets==17.0.1
